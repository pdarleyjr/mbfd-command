# Approved radio benchmark fixtures

Place operator-approved, de-identified WAV clips here and create `manifest.jsonl` beside them. Each line must contain `audio`, an exact human-reviewed `reference`, and the expected canonical `units`. Raw audio is intentionally excluded from Git.
